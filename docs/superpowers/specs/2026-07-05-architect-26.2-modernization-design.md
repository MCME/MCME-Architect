# MCME-Architect — Paper 26.2 Modernization Design

- **Status:** Approved design (pre-implementation)
- **Date:** 2026-07-05
- **Branch:** `architect-rework-2026`
- **Baseline:** v2.10.6 (`master` @ `8049b16`); Phase 0 hotfixes already committed on this branch.
- **Related docs:** [`audit.md`](../../../audit.md) (full findings), [`AUDIT-REMEDIATION-PLAN.md`](../../../AUDIT-REMEDIATION-PLAN.md) (phased plan).

## 1. Goal & context

Modernize MCME-Architect to run on **Paper 26.2** for a **coordinated release** alongside the new Perks plugin and the new TheGaffer. This is not a bare version bump: alongside the migration we fold in the audit's **High/Medium security & correctness fixes** ("migrate + stabilize"). TheGaffer already made the same 26.x jump, so its migration serves as a template.

**In scope:** 26.2 migration (including any API changes 26.x forces), removal of ProtocolLib in favour of Paper-native APIs, a spike-gated look at removing the PluginUtils dependency (spike ran in Wave A: **partial** result — `/checknbt` goes native, the copy/paste engine stays on PluginUtils, see §6), the audit's High/Medium fixes, and a MockBukkit test harness.

**Out of scope (deferred):** Conversation→Dialog migration; cosmetic/dead-code cleanup and logging normalization (audit Low/hygiene); deep new-API adoption (Display Entities, broad Dialog UIs); Low-severity audit items.

## 2. Locked decisions

| Decision | Choice |
|---|---|
| Target runtime | Paper **26.2** (compile against `26.1.2-stable`, run on the 26.2 dev server) |
| Build toolchain | **Stays Maven** — Architect has no direct NMS (all delegated to PluginUtils today), so no paperweight/Gradle needed |
| Scope | Migrate + **stabilize** (audit High/Medium) |
| ProtocolLib | **Removed** — 2 uses migrate to Paper-native |
| WorldEdit | **Kept**, but **blocked**: no 26.x-compatible build exists upstream yet (Wave A spike, §11); stays pinned at `7.3.0` until one ships |
| ViaVersion | Kept (`[5.0.0,6.0.0)` range; confirm it covers 26.x) |
| PluginUtils | **Partially removed** this release (spike ran, §6): `/checknbt` goes native, but the copy/paste engine (`MCMEPlotFormat`/NMS) is **NO-GO** to vendor or drop — dependency stays for that piece |
| MCME-Connect | Kept; consume its 26.2 build (migrating in parallel) |
| Conversations | **Kept** (no Dialog migration); tripwire if 26.x removed the API |
| Testing | **MockBukkit** harness, focused coverage on migration-critical paths + the fixes |
| Sequencing | Approach 1: **Foundation → runnable → stabilize-in-slices** |

## 3. Dependency plan

| Dependency | Now | Plan | Notes |
|---|---|---|---|
| `io.papermc.paper:paper-api` | 1.21.4 | → `26.1.2-stable` (provided) | Dialog API present at 26.1.2; run on 26.2 |
| `com.mcmiddleearth:PluginUtils` | 1.9.2 (hard depend) | **kept** (partial) | `/checknbt` goes native; `MCMEPlotFormat`/NMS copy-paste engine NO-GO to vendor — see §6. Also note: `1.9.2` itself is not yet cached locally (only `1.9.0`/`1.9.1`) |
| `com.mcmiddleearth:MCME-Connect` | 1.4.5 (provided) | → 26.2 build | Parallel migration; **blocker** until ready — confirmed absent from `.m2` (only a `.lastUpdated` stub for `1.5.1`) as of Wave A |
| `com.sk89q.worldedit:worldedit-*` | 7.3.0 | **blocked** — stays `7.3.0` | Wave A spike: latest release `7.4.4` and current upstream `master` still pin Paper API `1.21.4`/`1.21.5`; no 26.x Bukkit-platform build exists yet |
| `com.viaversion:viaversion-api` | `[5.0.0,6.0.0)` | keep range | Confirm 5.x covers 26.x |
| `com.comphenix.protocol:ProtocolLib` | 4.8.0 | **removed** | See §5.3 |
| MockBukkit + JUnit 5 | — | **added** (test) | MockBukkit version matched to the 26.1.2 paper-api registry, or `mock()` throws |

`plugin.yml`: **keep** `PluginUtils` in `depend` (removal is NO-GO this release, §6); keep the Phase-0 softdepends (`ProtocolLib` will be removed from softdepend too); update `api-version` (currently `1.19`) to the correct Paper 26.x value.

## 4. Approach — three waves

Approach 1 gets a **runnable 26.2 build early** (for integration against Perks/TheGaffer) and stands the **test harness up before** the risky security fixes. Cost: a few files are touched twice.

Order: **Foundation** (build + harness) → **Wave A** (compile) → **Wave B** (runnable) → **Wave C** (stabilize slices). Wave A proceeds against whatever PluginUtils/MCME-Connect builds exist; every point where Architect needs a not-yet-ready 26.2 lib API becomes an explicit tracked blocker.

## 5. Wave A — compile on 26.2

Goal: `mvn compile` green against `26.1.2-stable`, and the test harness in place.

### 5.1 Foundation
- Repoint `paper-api` to `26.1.2-stable`; confirm JDK target (21 today; 26.x may require newer — JDK 25 available locally). **Confirmed in Wave A:** paper-api 26.1.2 class files are major version 69 (Java 25); `maven.compiler.source/target/release` bumped 21 → 25.
- Stand up MockBukkit + JUnit 5, `src/test` tree, version matched to the 26.1.2 registry. **Confirmed in Wave A:** `org.mockbukkit.mockbukkit:mockbukkit-v26.1.2:4.114.0`, cross-verified against TheGaffer's identical pin.

### 5.2 Forced API migration
- **Material/enum renames** — sweep for renamed/removed constants (e.g. `GRASS`→`SHORT_GRASS`) across special-block and blockData code.
- **Removed/deprecated APIs** catalogued in the audit — `ItemStack` durability + `(mat,1,short)` constructor, legacy sign `setLine`, and any 1.20→26.x removals → modern equivalents (data components / `Damageable` / Side API).
- **protocolVersions.yml refresh** — add 768/769, fix the 1.21.2 mapping (audit Medium); required for RP client detection on 26.x.

### 5.3 ProtocolLib removal → Paper-native
- **Sign editor** (`SpecialBlockSign`, `OPEN_SIGN_EDITOR`) → `Player#openSign` (clean swap).
- **View distance** (`ViewDistanceListener`, LOGIN/UNLOAD_CHUNK/VIEW_DISTANCE packet manipulation) → Paper Player API. **Verify-early:** the current code does chunk-unload packet tricks that may lack a 1:1 native equivalent; acceptable outcomes are a small behaviour change or a documented stopgap.

### 5.4 Verify-early items (Wave A gates)
1. **Conversation API availability** — if 26.x removed Bukkit Conversations, "keep Conversations" is impossible; **stop and escalate** (do not silently do the deferred Dialog migration). Affects the custom-inventory editor prompts, `RegionEditConversation`, and `confirmation.*`.
2. **View-distance native equivalent** (§5.3).
3. **JDK target + `api-version`** for 26.x.
4. **WorldEdit 26.2** version availability.
5. **PluginUtils-removal spike** (§6).
6. **MockBukkit registry match**.

## 6. PluginUtils removal (spike-gated workstream) — SPIKE RESULT: NO-GO for full removal

Removing PluginUtils would cut one of the two "migrating in parallel" blockers and the shared-lib coupling. Usage surface: ~65 import sites, overwhelmingly trivial helpers. **The Wave A spike (see [findings doc §Task 6](2026-07-05-architect-26.2-wave-a-findings.md)) ran both unknowns below and found a split result: `/checknbt` is GO, but the copy/paste engine is NO-GO.**

**Spike (Wave A gate) — the two unknowns, as run:**
- `/architect checknbt` (op-only) calls `AccessInventory.getItemNBT` — the only live NMS use in that command. **Confirmed GO**: `ItemStack.getDataTypes()` + `toString()`/`getPersistentDataContainer()` (native 26.x data-component/PDC API) produce an equivalent op-readable dump.
- `plotStoring.IStoragePlot` (copy/paste) vendors cleanly. **Confirmed NO-GO for the full path.** The `IStoragePlot` interface itself, and most of the `plotStoring` package (`MCMEEntityFilter`, `StoragePlotSnapshot`, `EmptyStoragePlotSnapshot`, `PlotStorageFormat`, `LocalTransformation`, `EntityTypeUtil`, `InvalidRestoreDataException`, `BlockUtil`) vendor cleanly as plain Bukkit-API-only source. **But `MCMEPlotFormat` — the actual save/load engine `Clipboard` calls to serialize plot contents — is deeply, load-bearingly coupled to NMS** (`com.mcmiddleearth.pluginutil.nms.*`: `AccessCraftBukkit`, `AccessNBT`, `AccessWorld`, `AccessCore`, `AccessServer`, including direct `org.bukkit.craftbukkit.*` OBC casts) for reading/writing tile-entity and entity NBT with rotation/flip transforms. This is not dead code or droppable: it is how Architect's copy/paste, undo, and redo actually preserve chests, signs, item frames, paintings, and armor stands. There is no native data-component replacement for raw NBT placement with position/rotation transforms, and dropping copy/paste to enable a dependency removal is not acceptable (unlike the optional `/checknbt` debug command).

**Decision: defer full removal, keep PluginUtils for `MCMEPlotFormat`/`nms.*`.** Per the spike's own decision rule ("only a hard need for NMS with no native/drop option is NO-GO"), this is exactly that condition for the copy/paste half. Plan 2 should:
- Apply the `/checknbt` native swap independently (it does not require deferring — it has no coupling to the plotStoring finding).
- **Not** attempt full PluginUtils removal this release. Continue depending on PluginUtils' 26.2 build (once available — see §11 risks) for `MCMEPlotFormat` and its `nms.*` support.
- Optionally, as a smaller cut-candidate win: still vendor the ~64 trivial helper call sites (`NumericUtil`, `FileUtil`, `message.*`, `EventUtil`, `WEUtil`, `LegacyMaterialUtil`, `developer.DevUtil/Debugable`, `confirmation.*`, `VoxelUtil`/`ReflectionUtil`) and the now-confirmed-clean `plotStoring` supporting classes (everything except `MCMEPlotFormat`), shrinking — but not eliminating — the PluginUtils dependency surface to just the NMS-serialization piece. This is optional polish, not required for Wave A/B/C to proceed, and is itself a cut candidate if the timeline slips.

This workstream (the optional partial-vendor described above) remains a **cut candidate** if the timeline slips; full removal is off the table for this release regardless of timeline.

## 7. Wave B — runnable on the 26.2 dev server

- Loads cleanly (`api-version`, softdepends).
- Fix runtime issues the compiler can't catch: registry/enum mismatches, MockBukkit-vs-live differences, Dialog/Conversation runtime behaviour.
- **Dev-server smoke tests** for the things MockBukkit can't reach: RP switching, special-block place/break, editors, copy/paste, and the ProtocolLib-free **view distance**.
- Exit criterion: a 26.2 build stable enough to integrate against Perks and the new TheGaffer.

## 8. Wave C — stabilize in slices

Each slice = fix **+** MockBukkit tests (TDD-able now the harness exists). Phase 0 (armor-freeze, `/inv delete` auth, dependency guards, `/weselect`+`/sign` guards) is effectively the first slice, already committed.

1. **Path-safety helper** — one "resolve-and-verify-under-base-dir" utility applied to `/chead`, `/vv`, `/banner`, `/armor`, `/get head`.
2. **Load-guard** — "never save a config that failed to load"; skip bad files at enable. Targets: `CustomHeadData.fromFile`, `SpecialSavedInventoryData`, `SpecialBlockInventoryData`, `GetData`, `WorldConfig`, `ZipUtil` (destructive empty-download).
3. **Thread-safety & memory** — `/inv download` reload hop to main thread; `/chead submit` Mojang HTTP off the main thread; concurrent + quit-cleaned per-player maps; `refreshSHA` stream leak + async config write.
4. **Data-layer & placement correctness** — `RpDatabaseConnector` `client` column + `ResultSet`/`InputStream`/executor leaks; item-block NPEs and empty-`contentDamage` crash; cross-owner saved-inventory filename collision; item-frame duplication.
5. **Performance (lower priority, cut candidates)** — reduce per-`BlockBreakEvent` config-tree/blockList scans; cache the TheGaffer reflective `Method` (preserving the load-bearing `hasBuildPermission(Player, Location)` signature); `BlockPhysicsEvent` eager-allocation.

## 9. Testing strategy

- MockBukkit matched to the `26.1.2` paper-api registry; JUnit 5; new `src/test` tree.
- **Focused** coverage: the Wave C security/correctness fixes and migration-critical logic (special-block place/break, RP resolution, the vendored `NumericUtil`/`FileUtil`/`message` helpers). Not broad coverage.
- MockBukkit-unreachable paths (native view distance, real threading, Conversations/Dialog) → Wave B dev-server smoke tests.

## 10. Sequencing & parallel-lib blockers

- Foundation → A → B → C. Wave C slices can run near-parallel once B is green.
- MCME-Connect and PluginUtils **both** remain parallel dependencies: PluginUtils removal is NO-GO for the copy/paste engine (§6), so it stays a live dependency alongside MCME-Connect, not eliminated as originally planned.
- WorldEdit is now also a parallel-lib blocker (Wave A spike, §11): no 26.x build exists upstream, so Architect stays on `7.3.0` until EngineHub ships one.
- Any Architect change that needs a not-yet-shipped 26.2 lib API is logged as an explicit blocker rather than a silent stall.

## 11. Risks & open questions

| Risk | Handling | Wave A outcome |
|---|---|---|
| Conversations removed in 26.x | Verify first (§5.4.1); stop & escalate — do **not** auto-migrate to Dialog | **Resolved, no risk**: full Conversation API present in 26.1.2; kept as designed |
| View-distance has no native 1:1 | Verify (§5.3); accept small behaviour change or documented stopgap | **Confirmed, partially materialized**: LOGIN/VIEW_DISTANCE writes map cleanly to `setViewDistance`/`setSendViewDistance`; UNLOAD_CHUNK cancellation (chunk-unload suppression) has no native equivalent — accept behaviour change or find a different-mechanism stopgap in Wave B |
| `/checknbt` NMS not native-replaceable | Spike gate (§6); fall back to deferring PluginUtils removal | **Resolved, no risk for this piece**: native data-component/PDC dump confirmed GO |
| WorldEdit / ViaVersion 26.2 gaps | Verify versions in Wave A | **WorldEdit: materialized as a real blocker.** No 26.x-compatible release or `master`-branch build exists upstream (latest release `7.4.4` and current `master` both still pin Paper API `1.21.x`); stays on `7.3.0` until EngineHub ships one. ViaVersion range not yet separately re-verified against 26.x in this spike round. |
| MockBukkit registry mismatch | Match version to paper-api 26.1.2 | **Resolved, no risk**: `mockbukkit-v26.1.2:4.114.0` confirmed cached and cross-verified against TheGaffer's identical pin |
| PluginUtils copy/paste engine (`MCMEPlotFormat`) is NMS-coupled with no native/drop option | Not originally a distinct risk line — discovered during the §6 spike | **New, materialized risk.** Full PluginUtils removal is NO-GO this release; the dependency stays for `MCMEPlotFormat`/`nms.*`. See §6. |
| Timeline pressure | Full PluginUtils removal is off the table (not merely a cut candidate); the *optional partial* vendor of trivial helpers, and the Wave C performance slice, remain explicit cut candidates | |

## 12. Success criteria

1. `mvn` build green against `26.1.2-stable`; loads and core features work on the 26.2 dev server.
2. ProtocolLib gone; sign-editor works via native `Player#openSign`; view-distance LOGIN/runtime writes work via native `Player` API, with the chunk-unload-suppression gap either accepted as a documented behaviour change or covered by a different-mechanism stopgap.
3. PluginUtils dependency **partially** reduced: `/checknbt` goes native. Full removal is **not** a success criterion this release — the Wave A spike found the copy/paste engine (`MCMEPlotFormat`) NO-GO to vendor or drop; the dependency is explicitly kept for that piece, with a recorded reason (§6).
4. Audit High/Medium fixes landed with passing MockBukkit tests.
5. Conversations still functional as-is (Wave A spike confirmed no removal/escalation needed).
6. A build integrable with Perks and the new TheGaffer for the coordinated release — contingent on the MCME-Connect and WorldEdit 26.x blockers (§10, §11) resolving upstream; both are outside this repo's control and tracked as explicit blockers, not silently absorbed into scope.
